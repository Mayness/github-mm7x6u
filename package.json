{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "core",
      "subSystem1",
      "subsyStem2"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
